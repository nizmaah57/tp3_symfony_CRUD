<form method="post" action="{{ path('app_brand_crud_delete', {'id': brand.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger">Supprimer la marque</button>
</form>
