{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_brand_crud_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('app_brand_crud_edit', {'id': brand.id}) }}">Modifier</a>

    {{ include('brand_crud/_delete_form.html.twig') }}
{% endblock %}
