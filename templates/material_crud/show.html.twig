{% extends 'base.html.twig' %}

{% block title %}Material{% endblock %}

{% block body %}
    <h1>Material</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ material.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_material_crud_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('app_material_crud_edit', {'id': material.id}) }}">Modifier</a>

    {{ include('material_crud/_delete_form.html.twig') }}
{% endblock %}
