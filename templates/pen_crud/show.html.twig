{% extends 'base.html.twig' %}

{% block title %}Pen{% endblock %}

{% block body %}
    <h1>Stylo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pen.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pen.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ pen.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pen.description }}</td>
            </tr>
            <tr>
                <th>Ref</th>
                <td>{{ pen.ref }}</td>
            </tr>
            
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_pen_crud_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('app_pen_crud_edit', {'id': pen.id}) }}">Modifier</a>

    {{ include('pen_crud/_delete_form.html.twig') }}
{% endblock %}
