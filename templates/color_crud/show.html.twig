{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
    <h1>Color</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_color_crud_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('app_color_crud_edit', {'id': pen.id}) }}">Modifier</a>

    {{ include('color_crud/_delete_form.html.twig') }}
{% endblock %}
